---
interface Props {
  as?: "h1" | "h2" | "h3" | "h4" | "h5" | "h6";
  class?: string;
}

const { as: Tag = "h1", class: className } = Astro.props;
---

<Tag class:list={["font-semibold text-neutral-900 dark:text-white", className]}>
  <slot />
</Tag>
