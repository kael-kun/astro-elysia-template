---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["bg-white dark:bg-neutral-900 rounded-xl shadow-sm hover:shadow-md transition-shadow duration-200 p-6", className]}>
  <slot />
</div>
